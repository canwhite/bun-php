{
  "watch": ["src/", "scripts/"],
  "ignore": [
    "src/**/*.generated.ts",
    "src/**/*.test.ts",
    "src/**/*.test.tsx",
    "dist/",
    "node_modules/"
  ],
  "ext": "ts,tsx,js,json,css",
  "exec": "bun scripts/clean-port.js && bun scripts/clear-cache.js && bun run generate:islands && bun run generate:routes && bun run generate:api-routes && bun run build:css && bun run build:dev-reload && bun src/server.tsx",
  "env": {
    "NODE_ENV": "development"
  },
  "signal": "SIGKILL",
  "killTimeout": 5000,
  "verbose": true,
  "stdout": true
}
