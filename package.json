{
  "name": "bun-php",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@tailwindcss/postcss": "^4.1.18",
    "@tailwindcss/typography": "^0.5.19",
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^8.53.1",
    "@typescript-eslint/parser": "^8.53.1",
    "autoprefixer": "^10.4.23",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "eslint-plugin-prettier": "^5.5.5",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "nodemon": "^3.1.11",
    "postcss": "^8.5.6",
    "prettier": "^3.8.0",
    "tailwindcss": "^4.1.18"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.11.4",
    "minimatch": "^10.1.1",
    "preact": "^10.28.2",
    "preact-render-to-string": "^6.6.5"
  },
  "scripts": {
    "generate:islands": "bun scripts/generate-islands.ts",
    "generate:routes": "bun scripts/generate-routes.ts",
    "generate:api-routes": "bun scripts/generate-api-routes.ts",
    "build:css": "bun scripts/build-css.js",
    "build:dev-reload": "bun scripts/build-dev-reload.js",
    "dev:old": "bun run generate:islands && bun run generate:routes && bun run generate:api-routes && bun run build:css && bun run build:dev-reload && bun --hot src/server.tsx",
    "dev:watch": "bun run generate:islands && bun run generate:routes && bun run generate:api-routes && bun run build:css && bun run build:dev-reload && bun --watch src/server.tsx",
    "dev": "nodemon",
    "build:client": "bun run generate:islands && bun run generate:routes && bun run generate:api-routes && bun run build:css && bun run build:dev-reload && bun build ./src/entry-client.tsx --outdir ./dist --minify --target browser",
    "build": "bun run build:client",
    "start": "bun run generate:islands && bun run generate:routes && bun run generate:api-routes && bun src/server.tsx",
    "lint": "eslint . --ext .ts,.tsx --max-warnings 0",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "bun run lint && bun run format:check"
  }
}
